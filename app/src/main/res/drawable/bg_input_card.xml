<?xml version="1.0" encoding="utf-8"?>
<!-- Chat input card: white, rounded 16dp, blue border, subtle shadow -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Shadow layer -->
    <item
        android:left="0dp"
        android:top="4dp"
        android:right="0dp"
        android:bottom="2dp">
        <shape android:shape="rectangle">
            <corners android:radius="16dp" />
            <solid android:color="@color/fill_quaternary" />
        </shape>
    </item>
    <!-- Card surface -->
    <item>
        <shape android:shape="rectangle">
            <corners android:radius="16dp" />
            <solid android:color="@color/surface_raised" />
            <stroke
                android:width="1dp"
                android:color="#1f62ee" />
        </shape>
    </item>
</layer-list>
