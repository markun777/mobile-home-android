<?xml version="1.0" encoding="utf-8"?>
<!--
    Reusable shortcut tile — icon (36×36) + label (12sp)
    Usage: <include layout="@layout/item_shortcut" />
    Override drawable/app:srcCompat and text programmatically or define
    individual instances directly.
-->
<merge xmlns:android="http://schemas.android.com/apk/res/android">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:gravity="center_horizontal">

        <ImageView
            android:id="@+id/shortcut_icon"
            android:layout_width="@dimen/shortcut_icon_size"
            android:layout_height="@dimen/shortcut_icon_size"
            android:contentDescription="@null" />

        <TextView
            android:id="@+id/shortcut_label"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="4dp"
            android:textColor="@color/text_primary"
            android:textSize="12sp"
            android:gravity="center" />

    </LinearLayout>

</merge>
